// a // starts a comment
// use simple types, no const, reference or pointer.
// all used types must be possible to usable QDataStream
// you can use #include "file" statements, this will be written 1<>1 into the generated file
logMessage(QString data)
logError(QString data)
echo(QString text)
// a - in front means it is a reverse function Server->Client
-echoAnswer(QString text)
